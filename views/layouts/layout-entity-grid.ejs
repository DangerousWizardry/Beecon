<div class="entity" @click="clickEntity(entity)" v-for="entity in entities">
  <img src="/images/medic.png" alt="">
  <div class="entity-info">
    <span class="display-name">{{entity.entityDisplayName}}</span>
    <span class="last-received">Dernière reception {{ renderDurationName(entity.positions[entity.positions.length -1].timestamp) }}</span>
  </div>
  <i class="fa fa-2x fa-signal" :class="renderSignal(entity.positions[entity.positions.length -1].timestamp)"></i>
</div>